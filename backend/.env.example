# Backend Environment Variables - TEMPLATE
# Copy this file to .env and fill in your actual values
#
# For Docker deployment, these are set in docker-compose.yml
# This file is for local development outside Docker

# ==========================================
# Server Configuration
# ==========================================

# Node environment (development, production, test)
NODE_ENV=development

# Server port (default: 3000)
PORT=3000

# ==========================================
# Database Configuration
# ==========================================

# PostgreSQL connection string (used by some ORMs)
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://healthcare_user:healthcare_pass@localhost:5432/healthcare_ai_db

# Individual database connection parameters
# For Docker: use 'postgres' as host (service name)
# For local: use 'localhost' or '127.0.0.1'
DB_HOST=localhost
DB_PORT=5432
DB_NAME=healthcare_ai_db
DB_USER=healthcare_user
DB_PASSWORD=healthcare_pass

# Connection pool configuration (optional)
# Maximum number of connections in pool
DB_POOL_MAX=10

# ==========================================
# AI Service Configuration
# ==========================================

# Anthropic API Key (Claude AI)
# Get your key from: https://console.anthropic.com
# REQUIRED for AI risk analysis features
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here

# Claude model version (optional, defaults to claude-3-5-sonnet-20241022)
# CLAUDE_MODEL=claude-3-5-sonnet-20241022

# AI request timeout in milliseconds (optional, default: 30000)
# AI_TIMEOUT=30000

# ==========================================
# CORS Configuration
# ==========================================

# Allowed origins for CORS (comma-separated)
# Development: Include frontend dev server
CORS_ORIGIN=http://localhost:5173

# For production or multiple origins:
# CORS_ORIGIN=http://localhost:8080,http://localhost:5173

# ==========================================
# Optional: Advanced Configuration
# ==========================================

# Logging level (error, warn, info, debug)
# LOG_LEVEL=info

# Maximum request body size (default: 10mb)
# MAX_REQUEST_SIZE=10mb

# Rate limiting (requests per minute per IP)
# RATE_LIMIT=100

# ==========================================
# Instructions
# ==========================================

# 1. Copy this file to .env in the backend directory:
#    cd backend
#    cp .env.example .env
#
# 2. Update ANTHROPIC_API_KEY with your actual API key
#
# 3. For local development, set DB_HOST=localhost
#    For Docker, set DB_HOST=postgres
#
# 4. Start backend:
#    npm run dev
#
# 5. Test connection:
#    curl http://localhost:3000/health
#    curl http://localhost:3000/api/db/health

# ==========================================
# Security Notes
# ==========================================

# - Never commit .env file to version control
# - Never share your .env file or API keys
# - Rotate API keys regularly
# - Use different keys for development and production
# - Keep database passwords strong and unique
