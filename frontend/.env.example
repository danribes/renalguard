# Frontend Environment Variables - TEMPLATE
# Copy this file to .env and fill in your actual values
#
# For Docker deployment, these are set in docker-compose.yml
# This file is for local development outside Docker
#
# IMPORTANT: Vite exposes only variables prefixed with VITE_ to the browser
# https://vitejs.dev/guide/env-and-mode.html

# ==========================================
# Backend API Configuration
# ==========================================

# Backend API URL (used by frontend to connect to backend)
# For local development: http://localhost:3000
# For Docker development: http://localhost:3000 (port mapped to host)
# For production: Update with actual backend URL
VITE_API_URL=http://localhost:3000

# ==========================================
# Development Configuration
# ==========================================

# Node environment (development, production, test)
# This is NOT exposed to browser (no VITE_ prefix)
NODE_ENV=development

# ==========================================
# Optional: Feature Flags
# ==========================================

# Enable debug mode (shows additional logging in console)
# VITE_DEBUG=false

# Enable mock data mode (uses hardcoded data instead of API)
# VITE_MOCK_MODE=false

# API request timeout in milliseconds
# VITE_API_TIMEOUT=30000

# ==========================================
# Optional: Application Configuration
# ==========================================

# Application title (displayed in browser tab)
# VITE_APP_TITLE=Healthcare AI Clinical Data Analyzer

# Application version (displayed in UI)
# VITE_APP_VERSION=1.0.0

# ==========================================
# Instructions
# ==========================================

# 1. Copy this file to .env in the frontend directory:
#    cd frontend
#    cp .env.example .env
#
# 2. Update VITE_API_URL if needed (default should work for local dev)
#
# 3. Start frontend dev server:
#    npm run dev
#
# 4. Access application:
#    http://localhost:5173

# ==========================================
# Important Notes
# ==========================================

# - Only variables prefixed with VITE_ are exposed to the browser
# - Never put secrets or API keys in VITE_ variables (they're public!)
# - Backend API keys should be in backend/.env, not here
# - Changes to .env require restarting the dev server
# - For production builds, use .env.production file

# ==========================================
# Security Notes
# ==========================================

# - Never commit .env file to version control
# - VITE_ variables are PUBLIC - visible in browser source
# - Never expose API keys or secrets via VITE_ variables
# - Authentication should be handled by backend, not frontend
