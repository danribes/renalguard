# Cloudflare Pages Configuration for RENALGUARD Frontend
# Deploy with: wrangler pages deploy dist --project-name=renalguard-frontend

name = "renalguard-frontend"
compatibility_date = "2026-01-11"

# Build settings (for reference - actual build done by CI/CD)
# command = "npm run build"
# output = "dist"

# Environment variables (embedded at build time with Vite):
# - VITE_API_URL = https://renalguard-ai.fly.dev (Fly.io backend)
# - NODE_VERSION = 20
